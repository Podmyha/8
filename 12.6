slovo = input(f"введите слово:")

preobraz = list(slovo)

perevert = preobraz[::-1]

if preobraz == perevert:
    print(f"палиндром {preobraz} равен {perevert}")
else:
    print(f"не палиндром {preobraz} Не равен {perevert}")
